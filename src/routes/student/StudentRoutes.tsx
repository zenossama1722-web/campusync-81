import React from 'react'
import { Routes, Route } from 'react-router-dom'
// Student specific routes can be added here in the future
// Currently all student functionality is in common routes

export const StudentRoutes: React.FC = () => {
  return (
    <Routes>
      {/* Student specific routes will go here */}
    </Routes>
  )
}